# Technical Architecture - Admin Panel Redux React

## 🏗️ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                      CLIENT BROWSER                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   React Application                        │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │              React Router                            │ │ │
│  │  │  ┌─────────────┬─────────────┬─────────────────────┐ │ │ │
│  │  │  │ UserLogin   │ AdminLogin  │ ErrorPage           │ │ │ │
│  │  │  │ UserSignup  │ Dashboard   │ (404 Handler)       │ │ │ │
│  │  │  │ UserHome    │ Profile     │                     │ │ │ │
│  │  │  └─────────────┴─────────────┴─────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │            Redux Store                               │ │ │
│  │  │  ┌───────────────────────────────────────────────┐  │ │ │
│  │  │  │ userSlice:                                    │  │ │ │
│  │  │  │ - user: { _id, name, email, image }         │  │ │ │
│  │  │  │ - token: JWT                                 │  │ │ │
│  │  │  │ - isLoggedIn: boolean                        │  │ │ │
│  │  │  └───────────────────────────────────────────────┘  │ │ │
│  │  │         (Persisted to localStorage)                  │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │         Axios HTTP Client                            │ │ │
│  │  │   baseURL: http://localhost:2323/api                │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
                             ↕ HTTP/HTTPS
                    (Vite Dev Server Port 1010)
└─────────────────────────────────────────────────────────────────┘
│                      BACKEND SERVER                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │         Express.js Application (Port 2323)               │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │              CORS Middleware                          │ │ │
│  │  │  (Only allows http://localhost:1010)                 │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │              Route Handlers                           │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ POST /api/auth/signup                           │ │ │ │
│  │  │  │ POST /api/auth/login                            │ │ │ │
│  │  │  │ GET /api/auth/profile/:id                       │ │ │ │
│  │  │  │ PUT /api/auth/profile/:id                       │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ POST /api/admin/login                           │ │ │ │
│  │  │  │ GET /api/admin/users (Protected)                │ │ │ │
│  │  │  │ PUT /api/admin/users/:id (Protected)            │ │ │ │
│  │  │  │ DELETE /api/admin/users/:id (Protected)         │ │ │ │
│  │  │  │ POST /api/admin/users/:id/upload (Protected)    │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │         Middleware Layer                             │ │ │
│  │  │  - JWT Verification (verifyToken)                   │ │ │
│  │  │  - Multer File Upload Handler                       │ │ │
│  │  │  - Body Parser                                      │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │          Controller Layer                            │ │ │
│  │  │  - userController.js (Signup, Login, Profile)       │ │ │
│  │  │  - adminController.js (Admin Management)            │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │          Model Layer (Mongoose ODM)                  │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ User Schema:                                     │ │ │ │
│  │  │  │ - _id (ObjectId)                                │ │ │ │
│  │  │  │ - name (String)                                 │ │ │ │
│  │  │  │ - email (String, unique)                        │ │ │ │
│  │  │  │ - password (String, hashed)                     │ │ │ │
│  │  │  │ - profileImage (String, optional)               │ │ │ │
│  │  │  │ - timestamps (createdAt, updatedAt)             │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌──────────────────────────────────────────────────┐ │ │ │
│  │  │  │ Admin Schema:                                    │ │ │ │
│  │  │  │ - _id (ObjectId)                                │ │ │ │
│  │  │  │ - email (String, unique)                        │ │ │ │
│  │  │  │ - password (String, plain text ⚠️)              │ │ │ │
│  │  │  │ - timestamps                                    │ │ │ │
│  │  │  └──────────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │         Database Layer                               │ │ │
│  │  │  MongoDB Connection via Mongoose                    │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                         ↓                                   │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │         File Storage                                 │ │ │
│  │  │  /Back-End/uploads/ (Profile Images)               │ │ │
│  │  │  /uploads/:filename (Public Route)                 │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
                             ↕
                    ┌──────────────────┐
                    │    MongoDB       │
                    │   Database       │
                    │                  │
                    │ Collections:     │
                    │ - users          │
                    │ - admins         │
                    └──────────────────┘
```

---

## 🔄 Data Flow Diagrams

### **User Signup Flow**

```
┌──────────────┐
│ User fills   │
│ signup form  │
└──────┬───────┘
       │ Form submission
       ↓
┌──────────────────────────┐
│ Frontend validates:      │
│ - Email format           │
│ - Password strength      │
│ - Name not empty         │
└──────┬───────────────────┘
       │ Valid data
       ↓
┌──────────────────────────┐
│ POST /api/auth/signup    │
│ { name, email, password }│
└──────┬───────────────────┘
       │ Axios request
       ↓
┌──────────────────────────────────────┐
│ Backend: userController.js           │
│                                      │
│ 1. Check duplicate email             │
│    ├─ Found? → Return 409            │
│    └─ Not found? → Continue          │
│                                      │
│ 2. Hash password (bcryptjs)          │
│                                      │
│ 3. Create user in MongoDB            │
│                                      │
│ 4. Generate JWT token (1h expiry)    │
│                                      │
│ 5. Return { token, user }            │
└──────┬───────────────────────────────┘
       │ Response with token
       ↓
┌──────────────────────────────────────┐
│ Frontend Redux Actions:              │
│ - setUser(user data)                 │
│ - setToken(token)                    │
│ - localStorage.setItem('root', ...)  │
│ - isLoggedIn = true                  │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Navigate to /home (User Dashboard)   │
└──────────────────────────────────────┘
```

### **Admin User Management Flow**

```
┌──────────────────────────────────────┐
│ Admin at /dashboard clicks           │
│ "View Users" or "Delete User"        │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Redux State Check:                   │
│ - Get token from store               │
│ - Check isLoggedIn                   │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ API Request with Authorization:      │
│ GET /api/admin/users                 │
│ Headers: { Authorization: token }    │
└──────┬───────────────────────────────┘
       │ Axios includes token
       ↓
┌──────────────────────────────────────┐
│ Backend Middleware:                  │
│ verifyToken.js                       │
│                                      │
│ 1. Extract token from header         │
│ 2. Verify JWT signature              │
│ 3. Decode token → get admin._id      │
│ 4. Attach req.user = decoded         │
│ 5. Continue to controller            │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Backend: adminController.js          │
│                                      │
│ fetchingUserData():                  │
│ 1. User.find({})                     │
│ 2. Query all documents               │
│ 3. Return 200 + userData             │
└──────┬───────────────────────────────┘
       │ Response with user array
       ↓
┌──────────────────────────────────────┐
│ Frontend Updates State:              │
│ - Update Redux store                 │
│ - Re-render Dashboard                │
│ - Display user table                 │
└──────────────────────────────────────┘
```

### **File Upload Flow**

```
┌──────────────────────────────────────┐
│ User selects profile image           │
│ on /profile/:id page                 │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Frontend:                            │
│ 1. Validate image file               │
│ 2. Create FormData object            │
│ 3. POST /api/admin/users/:id/upload  │
│    (multipart/form-data)             │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Backend Middleware:                  │
│ multer.js                            │
│                                      │
│ 1. Intercept multipart request       │
│ 2. Generate unique filename          │
│ 3. Save to /uploads/[filename]       │
│ 4. Return filename to controller     │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Backend Controller:                  │
│ updateUser():                        │
│                                      │
│ 1. Get filename from multer          │
│ 2. Update user.profileImage          │
│ 3. Save to MongoDB                   │
│ 4. Return updated user object        │
└──────┬───────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────┐
│ Frontend:                            │
│ 1. Update Redux store                │
│ 2. Image accessible at:              │
│    /uploads/[filename]               │
│ 3. Display image in profile          │
└──────────────────────────────────────┘
```

---

## 🔐 Authentication & Authorization

### **JWT Token Structure**

```javascript
// Token Creation
jwt.sign(
  { id: user._id },                    // Payload
  process.env.JWT_SECRET,              // Secret key
  { expiresIn: "1h" }                  // Options
)

// Token Format: Header.Payload.Signature
// Decoded Payload:
{
  "id": "507f1f77bcf86cd799439011",
  "iat": 1708000000,                   // Issued at
  "exp": 1708003600                    // Expires at
}
```

### **Protected Route Authentication**

```
Request → Check Authorization Header
          ↓
    Extract Token
          ↓
    Verify Signature (using JWT_SECRET)
          ↓
    Check Expiration
          ↓
    Valid? → Attach user to request → Continue
          ↓
    Invalid? → Return 401 Unauthorized
```

---

## 📦 Component Structure

### **Redux Store Organization**

```javascript
// Store Tree
store
├── reducer
│   └── user: persistedReducer
│       ├── user: {
│       │   _id: string,
│       │   name: string,
│       │   email: string,
│       │   profileImage?: string
│       }
│       ├── token: string (JWT)
│       └── isLoggedIn: boolean
│
└── State Persistence
    └── localStorage: {
        key: 'root',
        data: serialized store
    }
```

### **Redux Actions**

```javascript
// userSlice.js actions

setUser({
  _id: "123",
  name: "John",
  email: "john@example.com"
})
→ Payload stored, isLoggedIn = true

setToken({
  token: "eyJhbGciOiJIUzI1NiIs..."
})
→ Token stored in state

logoutUser()
→ user = null
→ token = null  
→ isLoggedIn = false
→ localStorage cleared
```

---

## 🛣️ API Request/Response Patterns

### **Successful Signup Response**

```javascript
// Request
POST /api/auth/signup
{
  "name": "John Doe",
  "email": "john@example.com",
  "password": "password123"
}

// Response (201 Created)
{
  "msg": "User signup successfully",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "_id": "507f1f77bcf86cd799439011",
    "name": "John Doe",
    "email": "john@example.com"
  }
}
```

### **Error Response Pattern**

```javascript
// Duplicate Email (409 Conflict)
{
  "msg": "User already exists"
}

// User Not Found (400 Bad Request)
{
  "msg": "The user is not exists"
}

// Server Error (500)
{
  "msg": "internal server error"
}
```

### **Protected Route Request**

```javascript
// Request with Authorization
GET /api/admin/users
Headers: {
  "Authorization": "Bearer eyJhbGciOiJIUzI1NiIs..."
}

// Response (200 OK)
{
  "msg": "user data fetched successfully",
  "userData": [
    {
      "_id": "507f1f77bcf86cd799439011",
      "name": "User 1",
      "email": "user1@example.com",
      "profileImage": "uploads/abc123def456"
    },
    ...more users
  ]
}
```

---

## 📊 Database Schema Details

### **User Document Structure**

```javascript
{
  _id: ObjectId("507f1f77bcf86cd799439011"),
  name: "John Doe",
  email: "john@example.com",          // Unique index
  password: "$2a$10$ABCD...",         // Hashed with bcryptjs
  profileImage: "uploads/file123",    // Optional
  createdAt: ISODate("2024-01-15T10:30:00Z"),
  updatedAt: ISODate("2024-01-15T10:30:00Z"),
  __v: 0                              // Version key
}
```

### **Admin Document Structure**

```javascript
{
  _id: ObjectId("507f1f77bcf86cd799439012"),
  email: "admin@example.com",         // Unique index
  password: "plain-text-password",    // ⚠️ SECURITY ISSUE
  createdAt: ISODate("2024-01-01T00:00:00Z"),
  updatedAt: ISODate("2024-01-01T00:00:00Z"),
  __v: 0
}
```

---

## 🔄 Request/Response Lifecycle

### **Complete User Login Flow**

```
1. BROWSER → FRONTEND (React)
   User types email/password in login form
   
2. FRONTEND → REDUX
   Store captures form data locally
   
3. FRONTEND → BACKEND (Axios)
   POST /api/auth/login
   Payload: { email, password }
   
4. BACKEND → CONTROLLER
   userController.userLogin()
   - Find user in MongoDB
   - Compare password (bcryptjs)
   - Generate JWT token
   
5. BACKEND → FRONTEND
   Response: { token, user }
   Status: 200 OK
   
6. FRONTEND → REDUX
   dispatch(setUser(userData))
   dispatch(setToken({ token }))
   
7. FRONTEND → LOCALSTORAGE
   Redux-Persist saves state
   
8. FRONTEND → ROUTER
   Navigate to /home
   
9. FRONTEND → COMPONENTS
   Render UserHome with user data
   All subsequent requests include token
```

---

## 🎯 Error Handling Strategy

### **Frontend Error Handling**

```javascript
try {
  const response = await api.post('/auth/login', credentials)
  dispatch(setUser(response.data.user))
} catch (error) {
  if (error.response?.status === 400) {
    // Show: "The user does not exist"
  } else if (error.response?.status === 401) {
    // Show: "Invalid credentials"
  } else if (error.response?.status === 409) {
    // Show: "User already exists"
  } else {
    // Show: "Server error, please try again"
  }
}
```

### **Backend Error Handling**

```javascript
try {
  // Business logic
  if (condition) {
    return res.status(400).json({ msg: 'Error message' })
  }
} catch (error) {
  console.log('Error:', error)
  return res.status(500).json({ msg: 'internal server error' })
}
```

---

## 🚀 Performance Optimization

### **Current Optimizations**
- ✅ Redux state caching
- ✅ localStorage persistence
- ✅ Static file serving for uploads
- ✅ Async/await for non-blocking operations

### **Recommended Optimizations**
- 🔄 Implement pagination for user lists
- 🔄 Add request debouncing for search
- 🔄 Cache API responses with React Query
- 🔄 Lazy load components
- 🔄 Compress images before upload
- 🔄 Enable gzip compression on backend

---

## 🔌 Environment & Configuration

### **Development Environment**

```bash
Frontend Ports:  http://localhost:1010 (Vite)
Backend Port:    http://localhost:2323 (Express)
Database:        mongodb://localhost:27017/admin-panel
Auth:            JWT (1 hour expiry)
File Storage:    /Back-End/uploads/
```

### **CORS Configuration**

```javascript
// Current Setup (Development)
cors({
  origin: 'http://localhost:1010'
})

// Should be changed to:
cors({
  origin: process.env.FRONTEND_URL,
  credentials: true
})
```

---

**Architecture Document Version:** 1.0  
**Last Updated:** February 2026
